---
summary: "CLI reference for `qcortex uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `qcortex uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
qcortex uninstall
qcortex uninstall --all --yes
qcortex uninstall --dry-run
```
