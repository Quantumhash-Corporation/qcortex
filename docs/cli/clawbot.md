---
summary: "CLI reference for `qcortex clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `qcortex clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `qcortex clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `qcortex clawbot qr` (same behavior as [`qcortex qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `qcortex clawbot qr` -> `qcortex qr`
